# subchecker-telegram-bot

Just a bot to check if the user subscribed to the main channel and approve the private channel request after unsubscribing from the main channel the user will be removed from the private channel

Run docker with the `-e TOKEN="YOUR_BOT_TOKEN"` option and then edit the `.env` file, or just build docker with the `.env` already edited
